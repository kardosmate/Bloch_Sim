# Bloch_Sim

A szerver indításához futtatni kell a `server.exe` fájlt.

Miután elindult, az alkalmazás elérhető lesz a **[http://localhost:8080](http://localhost:8080)** címen. Ha a 8080-as port foglalt, a szerver nem fog elindulni.
